base: base.yaml
method: dann
discriminator:
  hidden_dim: 256
  dropout: 0.5
grl_schedule:
  gamma: 10.0 # alpha(p) = 2 / (1 + exp(-10 p)) - 1
  max_alpha: 1.0 # main comparison; the controlled study scales the schedule's ceiling over {0.25, 0.5, 1}
loss:
  domain_loss_weight: 1.0
